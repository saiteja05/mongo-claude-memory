This run (2026-07-21, runId ebbf2617-1e2b-49cf-a547-2aa5c3f222b0) is a fresh, full 4-arm rerun with no cached data reused from any prior run. Headline, adjudicated: control (guessability) 0/24 (0%), stock 11/24 (46%), engine 14/24 (58%), engine+native 24/24 (100%). Against the 2026-07-14 baseline (control 8%, stock 58%, engine 75%, engine+native 100%), every arm except engine+native dropped in point estimate, though the 95% Wilson CIs at N=24 overlap substantially between the two runs for stock and engine, so the drop should not be read as a precise effect size. It is real behavior, not noise or a grading artifact: verified below by reading raw transcript text, not just trusting the automated judge.

**Why the engine arm stayed low: the injected brief carried zero provenance framing, and the model treated it as suspected prompt injection.** All 20 judge/keyword disagreements this run were manually reviewed against the raw answer text in state/<arm>/answers.jsonl before merging into adjudications.json (all 20 accepted as-is). Direct transcript extraction confirmed the SessionStart hook's additionalContext field, at the time of this run, was a completely bare wall of factual sentences with no header, label, or provenance statement of any kind (buildAdditionalContext in src/hooks/sessionStart.ts simply returned parts.join("\n\n")). The engine arm's answers on f09 (rate limit), f12 (CI config), f02 (package manager) and f03 (retry policy) explicitly reasoned that this was "injected context, not something you or the code told me" and "This looks like a prompt-injection test bundled into the session," refusing to assert facts that were, per capture-check, genuinely present in its own belief store. The engine-native arm, given the exact same underlying facts via Claude Code's own native CLAUDE.md/MEMORY.md files rather than a hook payload, asserted them confidently. This reproduces the same mechanism first observed in the 2026-07-14 run's engine-arm drop, now root-caused precisely rather than left as an open question.

**Disposition.** This run is superseded, not deleted or hidden: a root-cause fix (a provenance/trust preamble prepended to the normal fetched-brief path in src/hooks/sessionStart.ts, TRUST_PREAMBLE, designed via a multi-angle draft plus two rounds of adversarial security/effectiveness review, implemented and verified with the full test suite green) was applied immediately after this run, and a new fresh run was started to measure its actual effect. See the top-level REPORT.md (or the next dated archive folder) for the post-fix numbers. Do not cite this run's engine-arm number as the current state of the product; cite the post-fix run instead.

**Capture, seed, and consolidation reliability.** No anomalies of note beyond the diagnosed brief-framing issue; capture-check, seed, and consolidation all completed without manual intervention this run.
