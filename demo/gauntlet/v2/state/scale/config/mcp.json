{
  "mcpServers": {
    "mongo-claude-memory": {
      "command": "node",
      "args": [
        "/Users/teja.boddapati/Desktop/mongo-claude-memory/dist/mcp/server.js"
      ],
      "env": {
        "MEMORY_MONGODB_DB": "claude_memory_gauntlet_v2_scale"
      }
    }
  },
  "_note": "Present for parity with the real engine-arm config (see setup.mjs). This scenario's recall trials do not pass --mcp-config, so memory_search is never an allowed tool: the point is to measure whether the SessionStart-injected brief alone carries the fact, not whether the model can proactively search memory."
}
